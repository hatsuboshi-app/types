/**
 * The source of which a skill card is obtained from.
 */
enum SkillSource {
    PIdol = "pIdol",
    SupportCard = "supportCard",
    Basic = "basic",
    Other = "other"
}

export default SkillSource
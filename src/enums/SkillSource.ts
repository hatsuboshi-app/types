/**
 * The source that a skill card can be obtained from.
 */
enum SkillSource {
    PIdol = "pIdol",
    SupportCard = "supportCard",
    Basic = "basic",
    Other = "other"
}

export default SkillSource